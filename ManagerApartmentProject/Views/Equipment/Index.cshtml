@model IEnumerable<Equipment>

<a asp-action="Create" class="btn btn-default">Create</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th> @Html.DisplayNameFor((model) => model.ID) </th>
            <th> @Html.DisplayNameFor((model) => model.name) </th>
            <th> @Html.DisplayNameFor((model) => model.count) </th>
            <th> @Html.DisplayNameFor((model) => model.apartmentName) </th>
            <th> Action </th>
        </tr>
    </thead>
    <tbody>
        @{
            string[] tmp = {"Chờ phê duyệt", "Đang xử lý", "Hoàn thành"};
        }
        @foreach (var item in Model){
            <tr>
                <td> @Html.DisplayFor(model => item.ID) </td>
                <td> @Html.DisplayFor(model => item.name) </td>
                <td> @Html.DisplayFor(model => item.count) </td>
                <td> @Html.DisplayFor(model => item.apartmentName) </td>
                
                <td>
                    <a asp-action="Detail" asp-route-id="@item.ID" class="btn btn-info btn-sm btn-small">
                        <i class="fas fa-eye"></i>
                    </a>
                    <a asp-action="Update" asp-route-id="@item.ID" class="btn btn-danger btn-sm btn-small">
                        <i class="fas fa-pencil-alt"></i>
                    </a>
                    <a  asp-action="Delete" asp-route-id="@item.ID" class="btn btn-warning btn-sm btn-small">
                        <i class="fas fa-trash"></i>
                    </a>
                </td>

            </tr>
        }
    </tbody>
</table>


@section Css {
    <link rel="stylesheet" href="~/fontawesome-5.15.3/css/all.css">
    <style>
        .btn-small {
            padding: 0.3rem 0.3em 0.3em 0.5em;

        }
        tbody {
            max-height: 55vh;
            overflow: auto;
        }
        tbody ::-webkit-scrollbar{
            width: 5px;
        }
        tbody::-webkit-scrollbar-track{
            background: transparent !important;
        }

        tbody::-webkit-scrollbar-thumb:hover{
            background-color: rgb(180, 179, 179);
        }
    </style>
}
